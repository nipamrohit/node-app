# Node Deployment App

This is a basic Node.js application created for learning CI/CD using Jenkins and Docker.

## How to Run
1. Install Node.js
2. Run `npm install`
3. Run `npm start`
4. Open browser at http://localhost:3000
